services:
  - type: web
    name: ishlarbot
    env: node
    buildCommand: pnpm install --frozen-lockfile && pnpm build
    startCommand: node dist/index.js
    nodeVersion: 18
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
