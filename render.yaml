services:
  - type: web
    name: ishlarbot
    env: node
    buildCommand: pnpm install --frozen-lockfile
    startCommand: pnpm start
    nodeVersion: 18
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
