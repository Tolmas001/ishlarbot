version: '3.8'

services:
  bot:
    build: .
    container_name: ishlarbot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    ports:
      - "3000:3000"
